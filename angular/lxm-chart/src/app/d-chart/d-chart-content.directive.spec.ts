import { DChartContentDirective } from './d-chart-content.directive';

describe('DChartContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DChartContentDirective();
    expect(directive).toBeTruthy();
  });
});
